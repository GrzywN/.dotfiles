# GrzywN's neovim config

## Usage

Everything should just work, extra configuration is needed for:

- Vue + TypeScript:
```bash
npm install
```

## Formatters

Install needed formatters via mason.nvim and set them in conform.nvim

**Formatters are optional**
